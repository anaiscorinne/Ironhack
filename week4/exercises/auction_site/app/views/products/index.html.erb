<ul>
<% @products.each do |product| %>
	<li>
		<%= link_to("#{product.title}", user_product_path) %>
		<br>
		<%= product.description %>
		<br>
		<%= product.deadline %>
		<br>
		<%= link_to("Destroy", user_products_path(@user, product), method: :delete, data: { confirm: "Are you sure?"}) %>
		<br><br>
	</li>
<% end %>
</ul>